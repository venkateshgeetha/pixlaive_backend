const ErrorMessage = {
    general: {
        required: 'Field is Required',
        validation: 'Not a valid value'
    },
    id: {
        required: 'Field is Required',
        validation: 'Not a valid value'
    }
}

module.exports = {ErrorMessage}